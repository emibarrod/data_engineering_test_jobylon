name: 'test_jobylon'
version: '1.0.0'

profile: 'test_jobylon'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  test_jobylon:
    staging:
      +materialized: view
    intermediate:
      +materialized: table